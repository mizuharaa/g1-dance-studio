# HANDOFF — 2026-08-05 (for the incoming agent)

**Start here, then read `docs/PROJECT_STATE.md` (decision log) and `CLAUDE.md` (rules).**
This handoff captures the state of the project after a ~2-week gap: the last commit
before this one was 2026-07-22 (`265506d`, "attempt 10 (v13 style) launched").
Nothing was recorded between then and now.

---

## TL;DR — where the project stands

- **Best policy ever: v12-final (attempt 9)** — 8/9 honest-gate PASS on the harness-v2
  gate (first trustworthy numbers of the project). Clean survival 100%, drift ep-p95
  0.91 m (PASS 1.5), ankle p95 14.3 (inside the real robot's measured 15–19 band),
  rr_mpkpe 0.057 (best). Artifacts: `exports/train-thriller_v12fid-0722/`
  (policy.onnx + policy_meta.json + deploy npz + gap.json). Published as dance
  `20260722-a8908992` in the app.
  **Sole gate FAIL:** the full composite `dr_delay40ms_push` row — 57% vs bar 95%
  (each factor alone passes: noise 100%, delay40 100%, push-only 89.8%, DR-only 93.8%).
- **Attempt 10 (v13 style revamp) was launched 2026-07-22 on box 40711 and its fate is
  UNKNOWN** — no result, no artifacts, no log entry after launch. See problem #1.
- **The robot is still broken** (burnt DC-DC converter, 2026-07-15) → deploy is blocked
  regardless of policy quality. Training/sim track is unaffected.
- The v12-final preview still shows a **style gap** the user rejected: twitching legs,
  under-reaching legs, visible (in-bounds) drift. v13 was the attempt to fix it.

## Biggest problems to fix RIGHT NOW (in order)

1. ~~GreenNode box audit~~ **RESOLVED 2026-08-05: user confirms the notebook
   instances were deleted long ago** → no runaway box billing, and the v13
   (attempt 10) artifacts are LOST with box 40711. REMAINING CHECK: the
   **`g1dance-data` Network Volume** is separate from the notebooks and bills while
   it exists — user should verify in the console (Storage management → Network
   Volume) that it's deleted too, or delete it if no run is planned soon
   (contents are reproducible from the repo). Billing history should be flat
   since the deletion date.
2. **Re-run v13 (attempt 10) — artifacts confirmed lost.** The recipe is fully
   committed and reproducible: `ffcf6d2` —
   v13 = v11 recipe + hip/knee per-channel action-rate (−0.03) + leg action stds
   0.26/0.34, trained on the certified `data/motions/thriller/v12_bundle/`
   (bundle_id 2595068f9243…, hash-verified), same 22/25 ankle + drift-p95-1.5 bars,
   RUN_NAME=train-thriller_v13style-0722. Launcher chain: `cloud/run_attempt9.sh`
   pattern + `logs/jobs.md` 2026-07-22 entries describe the exact setup.
3. **Robot repair (deploy blocker).** DC-DC buck converter on the power board burnt
   (diagnosed 2026-07-15; battery disconnected, do NOT repower). RMA email to
   support@unitree.com drafted in the user's Gmail 2026-08-05 — user must add the
   serial number + order info and send. Until repaired there is no hardware calibration
   point — and real-robot behaviour is the final arbiter for both the style gap and
   the drift question.
4. **Close the composite-robustness gap** (`dr_delay40ms_push` 57% vs 95). Per-factor
   rows are strong, so the gap is interaction effects. Note the old ~70%-IRL anchor
   scored 34.4% on the *contaminated* version of this row — the bar itself may be
   stricter than deployed reality; re-derive the calibrated bar on harness v2 before
   spending GPU chasing it.
5. **Style gap is measured but unsolved.** `experiments/style_gap_20260722/`: leg
   command HF content 3.97% vs 1.2–2.3% real baseline, leg reach 60–79%, base sway
   3× too stiff, clean drift 0.53 m (passes the bar but visible). v13 targets this;
   judge it on those recorded metrics, which the gate now records (fix J).

## What this commit contains

- This handoff.
- `docs/PROJECT_STATE.md` updated: it was stale (its decision log stopped at the
  2026-07-21 audit and missed attempt 9's final result and the v13 launch — those
  lived only in `logs/jobs.md` and commit messages). Now reconciled.
- ~34 dance registrations (`data/dances/2026072*`) + sim previews + export metadata
  (`exports/train-thriller_v10spd-0720/`, `v11leg-0720/`, `v12fid-0722/` READMEs and
  policy_meta.json) that were generated on 07-20..07-22 but never committed.
  Binaries (mp4/onnx/npz) stay untracked per `.gitignore` — EXCEPT the v12-final
  `policy.onnx` + `thriller_v12fid_deploy.npz`, force-added later on 2026-08-05
  (c51f968) so the best policy survives this laptop; pull the repo and
  `exports/train-thriller_v12fid-0722/` is deployable as-is.

## Key facts you must not relearn the hard way

- **Gate numbers v5–v11 are NOT comparable to v12+** — harness v2 (audit fixes F–L)
  changed the measurement. v12-final is the new baseline.
- **The 2026-07-21 ML audit** (84-agent, `experiments/ml_audit_20260721/REPORT.md`)
  found 12 confirmed defects, all fixed: the training reference was pre-blunted, the
  preview lied (stale motion + 90° rotation), and the v8–v11 ankle recipe was largely
  inert/inverted. Watchlist of 12 immaterial PLAUSIBLEs in `PLAUSIBLE.md`.
- **Torque model absolute numbers are inflated** (6–10× vs real telemetry); it's a
  relative hot-spot detector only. Native-tempo training is validated.
- **`data/motions/thriller/thriller_v12_full.csv` is DEPRECATED — never train on it.**
  Use `data/motions/thriller/v12_bundle/` (content-addressed, byte-reproducible).
- **pgrep self-match** has killed launches 3 times — always use `[b]racket` patterns.
- Pinned training env: `cloud/env_lock/requirements.lock.txt` (mjlab==1.5.0,
  mujoco-warp==3.10.0.1, warp-lang==1.14.0, torch cu128). `MUJOCO_GL=egl` only for
  render/verify, never during training (Warp CUDA conflict).
- Robot safety: no low-level commands without sim verification + explicit human
  confirmation; the G1 has NO hardware e-stop (remote B-damp + power switch only).
- Stale `worktree-agent-*` worktrees/branches were pruned 2026-08-05. One unmerged
  spike was preserved as branch `spike/hands-collision-vet` (073e699): a Hands R&D
  collision-vet gate — honest findings note GVHMR gives no hand data and the sim
  model is Dex3, not Inspire; use sparingly.

## Resume protocol

1. Read this file, then `docs/PROJECT_STATE.md` top entries, then `logs/jobs.md` tail.
2. `git log --oneline -20` for the recent narrative.
3. First action: problem #1 above (box audit with the user).
